# Run viewer

The run viewer allows you to view any test run in the Momentic app.
You can step through the test run and see the state of the UI at each step.
It also provides insights into the test run, such as the duration and queue time.
